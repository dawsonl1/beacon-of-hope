# Production credentials are in appsettings.Production.json on the server.
# For local development, copy this file to backend/.env and fill in real values.
# Double underscores (__) map to nested JSON config keys in .NET.
ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=intex2;Username=intex2;Password=your-password;SslMode=Prefer;TrustServerCertificate=true
