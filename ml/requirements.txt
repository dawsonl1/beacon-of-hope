# ML stack for Python 3.11+ (including 3.14).
#
# If `pip install` tries to *build* scikit-learn from source, you are on a Python
# version without a wheel for that sklearn release — use scikit-learn>=1.8 (wheels for 3.14).
#
# Recommended (avoids "user site" installs and permission issues):
#   cd ml
#   py -3.14 -m venv .venv
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt
#
scikit-learn>=1.8.0,<1.9
numpy>=1.26.4,<3
pandas>=2.2.2,<4
joblib>=1.4.2,<2
supabase==2.5.0
python-dotenv==1.0.1
statsmodels>=0.14.2,<0.15
