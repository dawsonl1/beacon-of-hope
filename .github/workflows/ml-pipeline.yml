name: ML Pipeline

on:
  schedule:
    # Nightly at 3:00 AM UTC (9 PM MT)
    - cron: '0 3 * * *'
  workflow_dispatch:
    # Manual trigger — click "Run workflow" in Actions tab to test now
    inputs:
      mode:
        description: 'Which step to run'
        required: true
        default: 'all'
        type: choice
        options:
          - all           # smoke test ETL + run inference
          - smoke-test    # ETL only (no DB writes)
          - predictions   # inference only (writes to DB)

jobs:
  ml:
    runs-on: ubuntu-latest

    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: ml/requirements.txt

      - name: Install dependencies
        run: pip install -r ml/requirements.txt

      - name: Smoke test ETL (build training frames)
        if: >-
          github.event_name == 'schedule' ||
          github.event.inputs.mode == 'all' ||
          github.event.inputs.mode == 'smoke-test'
        run: python -m ml.run_all

      - name: Run inference (write predictions to DB)
        if: >-
          github.event_name == 'schedule' ||
          github.event.inputs.mode == 'all' ||
          github.event.inputs.mode == 'predictions'
        run: python -m ml.run_predictions
